name: shawkit
description: main package-compose of shawkit
manager: 'npm'
# TODO 统一收集packageJson，统一修改
devDependencies:
  commitizen:
    name: commitizen
    version: 4.3.0
    description: 用于规范化Git提交消息的工具
  git-cz:
    name: git-cz
    version: 4.9.0
    description: 用于帮助开发者创建符合约定式提交（Conventional Commits）规范的 Git 提交消息
    packageJson:
      config:
        commitizen:
          path: ./node_modules/git-cz
  '@commitlint/config-conventional':
    name: '@commitlint/config-conventional'
    version: 19.2.2
    description: 预定义的配置包，用于 commitlint，它提供了一套基于约定式提交（Conventional Commits）规范的规则集
  '@commitlint/cli':
    name: '@commitlint/cli'
    version: 19.4.0
    description: 用于检查 Git 提交消息是否符合约定式提交（Conventional Commits）规范的命令行工具
  husky:
    name: husky
    version: 9.1.5
    description: 用于在 Git 操作（如 commit、push 等）之前或之后运行脚本的工具
    packageJson:
      scripts:
        prepare: npx husky install
# configurations:
#   test: standards/test.txt
# git-cz: standards/changelog.config.js
# '@commitlint/cli': standards/commitlint.config.js
# husky:
#   - standards/.husky/prepare-commit-msg
#   - standards/.husky/commit-msg
